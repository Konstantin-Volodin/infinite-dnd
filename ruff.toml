# Ruff configuration (extends pyproject.toml settings)
# This file exists for editors that look for ruff.toml specifically

# Most config is in pyproject.toml - this extends it for per-path overrides

[lint.per-file-ignores]
# Tests can have unused imports (fixtures) and magic values
"tests/**" = ["ARG", "PLR2004", "S101"]
# Run script can have print statements
"run_game.py" = ["T201"]
